import { FormInputComponent } from './form-input';
import { DisplayFormComponent } from './display-form';

export const ComponentsExport = [FormInputComponent, DisplayFormComponent];

export const Components = [...ComponentsExport];
